# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
modules:
  # deblacklist thunderbolt module for my dock
  - type: script
    snippets:
      - "sed -i '/thunderbolt/d' /usr/lib/modprobe.d/secureblue.conf"
